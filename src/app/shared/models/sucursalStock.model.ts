export interface SucursalStock {
    id: number;  // ID de la sucursal
    sucursalNombre: string; 
    sucursalId:number; // Nombre de la sucursal
    quantity: number;  // Cantidad disponible en esa sucursal
}
